<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Esame.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Esame"
    Title="Prodotti">

    <CollectionView ItemsSource="{Binding Products}"
                    Margin="10">

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Frame Padding="10" Margin="5" BorderColor="LightGray" CornerRadius="10">

                    <VerticalStackLayout>

                        <!-- Immagine cliccabile -->
                        <Image Source="{Binding Image}"
                               HeightRequest="120"
                               Aspect="AspectFit">
                            <Image.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type local:MainPage}}, Path=BindingContext.OpenDetailsCommand}"
                                    CommandParameter="{Binding .}" />
                            </Image.GestureRecognizers>
                        </Image>

                        <Label Text="{Binding Title}"
                               FontAttributes="Bold"
                               FontSize="18"
                               Margin="0,5,0,0" />

                        <Label Text="{Binding Price, StringFormat='€{0:F2}'}"
                               TextColor="Green"
                               FontSize="16" />

                    </VerticalStackLayout>

                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>

</ContentPage>
